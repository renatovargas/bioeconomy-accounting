project:
  type: manuscript
  output-dir: docs
  execute-dir: project

manuscript:
  article: index.qmd

format:
  html:
    comments:
      hypothesis: true
  docx: default
  # pdf: default
  
  # (other formats)

execute:
  freeze: true

editor: source

